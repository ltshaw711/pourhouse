// Library — PRD §7: browse the complete collection (search, filter, sort, view toggle)

export default function LibraryPage() {
  return (
    <main className="mx-auto max-w-5xl px-6 py-16">
      <h1 className="text-3xl font-semibold text-zinc-50">Library</h1>
      <p className="mt-2 max-w-prose text-zinc-400">
        The cocktail grid/list view, filters, and ingredient search land here.
      </p>
    </main>
  );
}
